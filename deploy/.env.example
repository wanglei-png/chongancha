# ============================================
# 宠安查 - 生产环境配置文件
# 部署前请修改以下所有占位符为实际值
# ============================================

# MySQL 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Wanglei940414!
DB_DATABASE=pet_health

# Redis 配置（如不使用可留空）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# 微信小程序配置
WX_APPID=wxd9270c231a37d28c
WX_SECRET=your_wx_secret

# JWT 配置
JWT_SECRET_KEY=change-me-to-a-random-secret-at-least-32-chars
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# 大模型 API 配置
LLM_API_KEY=your_llm_api_key
LLM_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# 微信支付配置
WX_MCH_ID=your_merchant_id
WX_API_KEY=your_wx_pay_api_key
WX_NOTIFY_URL=https://chongjicha.wlstudio.com.cn/api/payment/notify

# 应用配置
APP_ENV=production
APP_DEBUG=false
